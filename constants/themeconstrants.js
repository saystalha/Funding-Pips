
export const THEME_STORAGE_KEY = 'user-theme';

// Theme mode values
export const THEME_MODE = {
    LIGHT: 'light',
    DARK: 'dark',
};

// Default theme if no preference is found
export const DEFAULT_THEME = THEME_MODE.DARK;